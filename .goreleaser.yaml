version: 2

project_name: dotr

before:
  hooks:
    - go mod tidy

builds:
  - id: dotr
    main: .
    binary: dotr
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/lum1n/dotr/internal/version.Version={{.Version}}
      - -X github.com/lum1n/dotr/internal/version.Commit={{.Commit}}
      - -X github.com/lum1n/dotr/internal/version.Date={{.Date}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"

release:
  draft: false
